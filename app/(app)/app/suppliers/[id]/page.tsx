import { PagePlaceholder } from "@/components/page-placeholder";

export default function SupplierPage() {
  return (
    <PagePlaceholder
      title="Supplier intelligence"
      description="Supplier pages are a Pro feature and will query protected data only after a server-side entitlement check."
    />
  );
}
