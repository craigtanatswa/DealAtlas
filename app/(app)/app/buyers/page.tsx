import { PagePlaceholder } from "@/components/page-placeholder";

export default function BuyersIndexPage() {
  return (
    <PagePlaceholder
      title="Buyers"
      description="Buyer watchlists are a Pro feature. This route will query protected buyer data only after a server-side entitlement check."
    />
  );
}
