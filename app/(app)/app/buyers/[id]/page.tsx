import { PagePlaceholder } from "@/components/page-placeholder";

export default function BuyerPage() {
  return (
    <PagePlaceholder
      title="Buyer intelligence"
      description="Buyer pages are a Pro feature and will query protected data only after a server-side entitlement check."
    />
  );
}
