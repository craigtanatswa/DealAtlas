import { PagePlaceholder } from "@/components/page-placeholder";

export default function AppSearchPage() {
  return (
    <PagePlaceholder
      title="Search"
      description="Authenticated search will be implemented in a later goal. Free search still uses deal_previews only."
    />
  );
}
