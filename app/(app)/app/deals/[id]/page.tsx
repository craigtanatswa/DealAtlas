import { PagePlaceholder } from "@/components/page-placeholder";

export default function AppDealPage() {
  return (
    <PagePlaceholder
      title="Deal"
      description="Protected deal detail requires a server-side entitlement check before canonical tables are queried."
    />
  );
}
