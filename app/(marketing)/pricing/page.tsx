import { PagePlaceholder } from "@/components/page-placeholder";

export default function PricingPage() {
  return (
    <PagePlaceholder
      title="Pricing"
      description="DealAtlas Pro pricing will be configurable through Dodo product IDs in server environment variables. Product IDs are not hard-coded."
    />
  );
}
