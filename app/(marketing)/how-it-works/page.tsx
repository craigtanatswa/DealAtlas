import { PagePlaceholder } from "@/components/page-placeholder";

export default function HowItWorksPage() {
  return (
    <PagePlaceholder
      title="How it works"
      description="Discover sanitised opportunities, assess fit, then unlock buyer and source details after a verified Pro subscription."
    />
  );
}
