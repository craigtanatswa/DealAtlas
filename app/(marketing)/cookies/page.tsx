import { PagePlaceholder } from "@/components/page-placeholder";

export default function CookiesPage() {
  return (
    <PagePlaceholder
      title="Cookies"
      description="Cookie policy wording will be reviewed against the live product before launch."
    />
  );
}
