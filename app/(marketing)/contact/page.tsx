import { PagePlaceholder } from "@/components/page-placeholder";

export default function ContactPage() {
  return (
    <PagePlaceholder
      title="Contact"
      description="A public contact path will be added before launch. This route exists so the information architecture is in place."
    />
  );
}
