import { PagePlaceholder } from "@/components/page-placeholder";

export default function DealPreviewPage() {
  return (
    <PagePlaceholder
      title="Opportunity preview"
      description="This public preview route will load deal_previews only after search is implemented. Protected source data is never sent to free or anonymous clients."
    />
  );
}
